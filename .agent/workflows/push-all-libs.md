---
description: Push changes for all lib submodules simultaneously
---

// turbo-all

# Push All Libs

Use for cross-cutting changes (e.g. CI/config updates). Skips unchanged libs.

```bash
bash tools/push-all-libs.sh "<type>(<scope>): <subject>"
```
